{
  "name": "runeclaw-anchor-workspace",
  "version": "0.1.0-draft",
  "private": true,
  "description": "DRAFT/devnet Anchor workspace tooling — provides the TS test runner for programs/*/tests/*.ts (`anchor test`). Separate from the Python bot and the app/ web server, which have their own dependencies.",
  "license": "BUSL-1.1",
  "scripts": {
    "test": "ts-mocha -p ./tsconfig.json -t 1000000 programs/rclaw_staking/tests/**/*.ts",
    "typecheck": "tsc -p ./tsconfig.json"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1"
  },
  "devDependencies": {
    "@solana/spl-token": "^0.4.9",
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "chai": "^4.4.1",
    "mocha": "^10.3.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.4.5"
  }
}
