# Pinned dependencies for the CI test environment (.github/workflows/ci.yml).
# This mirrors the environment the known-failures baseline (tests/known_failures.txt)
# was generated in, so the baseline-diff gate is reproducible. Optional, heavy, or
# import-guarded extras (anthropic, mplfinance/matplotlib/pandas) are intentionally
# omitted — their absence is part of the baseline.
python-dotenv==1.2.2
pydantic==2.13.3
ccxt==4.5.56
python-telegram-bot==22.7
openai==2.38.0
numpy==2.3.5
aiohttp==3.14.1
websockets==16.0
cryptography>=48.0.1
Pillow>=10.3.0
redis>=5.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
PyJWT>=2.7.0
# Dev / test tooling
pytest==8.3.5
pytest-asyncio==0.24.0
pytest-timeout>=2.3.0
hypothesis==6.155.7
ruff==0.11.13
mypy==1.15.0
# CI quality gates (coverage, dependency audit, SAST)
pytest-cov>=6.0.0
pip-audit>=2.9.0
bandit>=1.8.0
